import { Footer as MantineFooter } from "@mantine/core";

const Footer = () => {
  return (
    <MantineFooter height={60} p="md">
      Application Footer
    </MantineFooter>
  );
};

export default Footer;
